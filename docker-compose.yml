services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cratekey
      POSTGRES_PASSWORD: cratekey
      POSTGRES_DB: cratekey
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
